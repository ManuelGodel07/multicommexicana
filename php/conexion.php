<?php
        $base = mysqli_connect("localhost", "root", "", "multicommexicana");
/****        $base->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
        $base->exec("SET CHARACTER SET UTF8");
    } catch (Exception $e) {
        die('Error' . $e->getMessage());
        alert("Linea del error" . $e->getLine());
    }
 *****/

?>